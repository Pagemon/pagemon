{
    "manifest_version": 3,
    "name": "Pagemon",
    "description": "Pagemon is a chrome extension that notify you when the page changes. Pagemon is a productivity extension. It is helpful when you want to perform multiple tasks on your machine without checking every few seconds some updates in a single page app.",
    "version": "1.0",
    "background": {
        "service_worker": "background.js"
    },
    "action": {
        "default_popup": "hello.html",
        "default_icon": "assets/icon.png"
    }
}